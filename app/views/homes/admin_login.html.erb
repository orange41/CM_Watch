<!-- app/views/homes/admin_login.html.erb -->
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>管理者ログイン</title>
    <link rel="stylesheet" href="<%= asset_path 'styles.css' %>">
</head>
<body>
    <header>
        <h1>管理者ログイン</h1>
    </header>
    <main>
        <%= form_with url: new_custom_admin_session_path, method: :post do %>
            <div>
                <%= label_tag :employee_number, '社員番号' %>
                <%= text_field_tag :employee_number, nil, required: true %>
            </div>
            <div>
                <%= label_tag :password, 'パスワード' %>
                <%= password_field_tag :password, nil, required: true %>
            </div>
            <div>
                <%= submit_tag 'ログイン' %>
            </div>
        <% end %>
    </main>
</body>
</html>
